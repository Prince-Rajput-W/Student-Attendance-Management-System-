# Student-Attendance-Management-System-
The Student Attendance Management System is a C-based console application that manages student records and attendance using file handling. It allows adding students, preventing duplicate roll numbers, marking attendance, and viewing records, with data stored persistently in a binary file.
